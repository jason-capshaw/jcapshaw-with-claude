# jcapshaw-with-claude

Personal site and blog built with Astro + MDX.

## Setup

```bash
cd site
npm install
npm run dev
```

## Stack

- **Astro** — static site generator, zero JS by default
- **MDX** — markdown with embedded interactive components
- **Astro Islands** — opt-in client-side JS only where needed
